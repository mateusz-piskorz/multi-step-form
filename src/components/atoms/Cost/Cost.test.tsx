import { render, screen } from '@testing-library/react';
// import { Cost } from './index';

// test('displays a cost with monthly period', () => {
//   const cost = 50;
//   const period = 'monthly';
//   render(<Cost cost={cost} period={period} />);

//   expect(screen.getByText(`$${cost}/mo`)).toBeInTheDocument();
// });

// test('displays a cost with yearly period', () => {
//   const cost = 40;
//   const period = 'yearly';
//   render(<Cost cost={cost} period={period} />);

//   expect(screen.getByText(`$${cost}/yr`)).toBeInTheDocument();
// });

// test('displays a cost with plus icon', () => {
//   const cost = 40;
//   const period = 'yearly';
//   render(<Cost cost={cost} period={period} plusIcon />);

//   expect(screen.getByText(`+$${cost}/yr`)).toBeInTheDocument();
// });

export {};
