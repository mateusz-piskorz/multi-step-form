import { screen, render } from '@testing-library/react';
// import { AddonItem } from './index';

// const CostProps = jest.fn();
// jest.mock('../../../Cost', () => ({
//   Cost: jest.fn((props) => CostProps(props)),
// }));

// const defaultProps = {
//   addon: {
//     cost: { monthly: 12, yearly: 120 },
//     description: 'description',
//     title: 'title',
//     name: 'largerStorage',
//   },
//   changeHandler: jest.fn(),
//   checked: false,
//   paymentPeriod: 'monthly',
// } as const;

// it('displays title', () => {
//   render(<AddonItem {...defaultProps} />);
//   expect(screen.getByText(defaultProps.addon.title)).toBeInTheDocument();
// });

// it('displays description', () => {
//   render(<AddonItem {...defaultProps} />);
//   expect(screen.getByText(defaultProps.addon.description)).toBeInTheDocument();
// });

// it('displays Cost component', () => {
//   render(<AddonItem {...defaultProps} />);
//   expect(CostProps).toHaveBeenCalledWith({
//     cost: defaultProps.addon.cost.monthly,
//     period: defaultProps.paymentPeriod,
//     plusIcon: true,
//   });
// });

// it('displays unchecked input by default', () => {
//   render(<AddonItem {...defaultProps} />);
//   expect(screen.getByRole('checkbox')).not.toBeChecked();
// });

// it('displays checked input', () => {
//   render(<AddonItem {...defaultProps} checked />);
//   expect(screen.getByRole('checkbox')).toBeChecked();
// });

it('', () => {});
